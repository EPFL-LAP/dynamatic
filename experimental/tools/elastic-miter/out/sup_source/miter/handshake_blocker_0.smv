-- handshake_blocker_0 : blocker(['bitwidth=0'])


MODULE handshake_blocker_0(ins_valid, ctrl_valid, outs_ready)
  DEFINE
  // output
  outs_valid := ins_valid & ctrl_valid;
  ins_ready := outs_ready & ctrl_valid;
  ctrl_ready := outs_ready & ins_valid;

