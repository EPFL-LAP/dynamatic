#include "muxForkSwap_rhs.smv"


MODULE bool_input_inf(nReady0)
    VAR outs : boolean;
    
    -- make sure outs is persistent
    ASSIGN
    next(outs) := case 
      outs_valid & !nReady0 : outs;
      TRUE : {TRUE, FALSE};
    esac;
    DEFINE outs_valid := TRUE;

MODULE sink_main (ins_valid)
  DEFINE ins_ready   := TRUE;

MODULE main

  VAR seq_generator_sel_in : bool_input_inf(muxForkSwap_rhs.sel_in_ready);
  VAR seq_generator_data_in : bool_input_inf(muxForkSwap_rhs.data_in_ready);

  VAR muxForkSwap_rhs : muxForkSwap_rhs (seq_generator_sel_in.outs, seq_generator_sel_in.outs_valid, seq_generator_data_in.outs, seq_generator_data_in.outs_valid, sink_out1.ins_ready, sink_out2.ins_ready);

  VAR sink_out1 : sink_main(muxForkSwap_rhs.out1_valid);
  VAR sink_out2 : sink_main(muxForkSwap_rhs.out2_valid);

