name: C/C++ CI

on:
  # Make sure that settings are set to require permission
  # to run workflows by external contributors!
  pull_request:
    branches: ["main"]

jobs:
  integration:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: build
        run: ./build.sh --release

      - name: integration-test
        run: python3 run_integration.py --list integration-test/integration_list.txt --ignore integration-test/ignored_tests.txt

