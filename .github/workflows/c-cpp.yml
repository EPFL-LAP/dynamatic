name: C/C++ CI

on:
  push:
    branches: ["github-actions-test"]

  # Make sure that settings are set to require permission
  # to run workflows by external contributors!
  pull_request:
    branches: ["github-actions-test"]

jobs:
  integration:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: build
        run: ./build.sh --release

      - name: integration-test
        run: python3 run_integration.py --list integration-tests/integration_list.txt --ignore integration-tests/ignored_tests.txt

