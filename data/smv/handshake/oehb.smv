MODULE oehb (ins, ins_valid, outs_ready)
VAR inner_oehb : oehb_dataless(ins_valid, outs_ready);
VAR outs : DATA_TYPE;
ASSIGN
init(outs) := 0;
next(outs) := case
                ins_ready & ins_valid : ins;
                TRUE : outs;
              esac;
DEFINE ins_ready := inner_oehb.ins_ready;