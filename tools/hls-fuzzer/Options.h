#ifndef DYNAMATIC_HLS_FUZZER_OPTIONS
#define DYNAMATIC_HLS_FUZZER_OPTIONS

#include <string>

namespace dynamatic {

struct Options {
  std::string dynamaticPath;
};

} // namespace dynamatic

#endif
